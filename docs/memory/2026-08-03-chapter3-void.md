# 2026-08-03 — 第三章《月之彼端》(Chapter 3: The Void)

## 做了什麼

- 地圖第三層 `月之裏側` (rows 88-119)：虛空 `v` (擋路的星空)、星石地板 `n`
  (島緣打亮做漂浮感)、星光橋 `:`、星辰水晶 `*`、核心祭壇 `E`、傳送門 `@`。
  入口平台設安全區 (SANCTUM)。
- 傳送門系統：`Portals` 表取代寫死的階梯判定，`MinQuest` 作劇情閘門
  (深淵之門在第三章前踩上去只會被擋 + 提示, 有節流)。
- 新怪：月靈 (漂浮半透明)、星衛 (稜甲開闔)；最終 Boss `月蝕根源`
  900HP，星爆 4.5u；`半血狂暴`: 變紅、速度 x1.5、冷卻減半、附帶召喚月靈。
- 劇情 S13-S19 + 傳說裝備 `Void Edge` (ATK34) / `Aegis of Dawn` (DEF16)。
- 視覺：極光帶 (lighter 混合三色流動)、視差星塵、狂暴鏡頭震動、
  Boss 血條 RAGE 標記與橙色化、樓板遮蔽推廣到第三層。

## 決策

- 多層仍是`同一張 ASCII 地圖`縱向分段 (row 界線常數)，層間走 Portals。
  碰撞/快照/AI 全部沿用單層邏輯，零額外狀態機。
- 狂暴是 Monster 上的一次性 flag (`enraged`)，經快照 `rg` 傳給 client 決定配色。

## 驗證

- `go test ./...` 全綠：E2E 從 S0 一路走到 S19 (三章全通)，新增
  `TestVoidPortalGate` (進度不足擋下 + 提示 + 達標放行) 與
  `TestCoreEnrage` (半血觸發 rage/tel/slam 事件 + 造成傷害)。
- CDP 真人流程實測：讀檔續玩 (quest16) → 地下層長征 → 踩門傳送進裏側 →
  抵達祭壇觸發 S17 → 實戰 900HP Boss → 420HP 時狂暴 (截圖) → 擊殺推進 S18。

## 踩雷

- 地下層通道 rows 68-70 只有 cols 27-28：自動走位必須`先對齊 x 再下行`,
  否則卡在岩壁前 (症狀是位置完全不動, 沒有錯誤)。
- row 85 的門扇 `G` 夾住傳送口 `@`：只能從 row 84 `垂直踏入`, 沿 row 85
  橫走會被門扇擋住。
- 長時間 screencast + 多次重跑會讓 headless Chrome 掛掉 (ws 斷線);
  拆成「旅程不錄影, 到場才開錄」並換 user-data-dir 重啟即可。
